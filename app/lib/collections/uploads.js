/*Uploads = new FS.Collection("uploads", {
  stores: [new FS.Store.FileSystem("uploads", {path: "~/uploads"})]
});

if(Meteor.isServer) {
  console.log('test');
  Uploads.allow({
    'insert': function () {
      return true;
    }
  });
}
*/
